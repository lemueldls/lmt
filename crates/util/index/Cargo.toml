[package]
name = "lmt-index"

edition.workspace = true
version.workspace = true

[lints]
workspace = true

[dependencies]
parking_lot = { version = "0.12.1", features = [
  "deadlock_detection",
  "nightly",
  # "send_guard",
  # "owning_ref",
  # "arc_lock",
], public = true }
slotmap = { version = "1.0", public = true }
